apiVersion: batch/v1
kind: Job
metadata:
  name: db-migration
  namespace: easyshop
spec:
  template:
    spec:
      containers:
      - name: migration
        # TODO (replica): replace with YOUR image built from scripts/Dockerfile.migration
        # and pushed to your registry (Docker Hub / ECR).
        image: trainwithshubham/easyshop-migration:1
        imagePullPolicy: Always
        env:
        - name: MONGODB_URI
          value: "mongodb://mongodb-service:27017/easyshop"
      restartPolicy: OnFailure
